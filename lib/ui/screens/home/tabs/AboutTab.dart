import 'package:flutter/material.dart';

// Screen permettant d'afficher une vue À propos
class AboutTab extends StatelessWidget {
  const AboutTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('À propos'),
    );
  }
}
